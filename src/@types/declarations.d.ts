/**
 * Define common `Types` here to be automatically avialable in both the
 * `/client/` and `/server/` directories.
 * 
 * A common example would be `Events` passed between the client and server.
 */

declare enum LogLevel {
  debug = 1,
  info = 2,
  warn = 3,
  error = 4,
}
